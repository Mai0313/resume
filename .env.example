# 環境變數範例檔案
# 複製此檔案為 .env.local 並根據需要修改

# GitHub API Token (用於 Portfolio 頁面)
VITE_GITHUB_TOKEN=...

# PIN 碼保護 (用於 Resume 頁面)
# 如果不設定或留空，則不需要 PIN 碼驗證
VITE_PIN_CODE=123456

# 自定義根路徑 (用於不同的部署環境)
# 預設值為 '/'，適用於部署在網域根目錄
# 如果部署在子目錄，請設定對應的路徑（例如：'/my-app'）
# VITE_ROOT_PATH=/

# 部署範例：
# 部署在 GitHub Pages 的專案子目錄：VITE_ROOT_PATH=/your-repo-name
# 部署在自定義子目錄：VITE_ROOT_PATH=/portfolio
# 部署在根目錄：VITE_ROOT_PATH=/ (或不設定此變數)
