# Environment Variables Configuration
# Copy this file as .env and modify as needed

# ===================
# REQUIRED VARIABLES
# ===================
# These variables MUST be set or the application will throw an error

# Website Configuration
VITE_WEBSITE_TITLE=Mai

# ===================
# OPTIONAL VARIABLES
# ===================
# These variables have default values and are optional

# Resume File Configuration
# Uses the rendercv YAML schema: https://docs.rendercv.com/user_guide/yaml_input_structure
# Supports both local files and remote URLs:
# - Local file: "resume.yaml" or "example.yaml"
# - GitHub Gist: "https://gist.github.com/username/gist_id"
# - Raw URL: "https://raw.githubusercontent.com/user/repo/main/resume.yaml"
# If not set, defaults to "example.yaml"
VITE_RESUME_FILE=https://raw.githubusercontent.com/Mai0313/resume/refs/heads/main/public/example.yaml

# Resume PDF Download Path
# Path to the PDF file served to users from the download button.
# The build pipeline generates this file from the YAML via `rendercv render`,
# so the default filename is "resume.pdf".
VITE_RESUME_PDF_PATH=resume.pdf

# PIN Code Protection (used for Resume page)
# If not set, PIN code verification is disabled
# VITE_PIN_CODE=123456

# Custom Root Path (used for deployment)
# If not set, defaults to "/" (root directory)
# VITE_ROOT_PATH=/your-repo-name

# ===================
# USAGE EXAMPLES
# ===================

# Resume File Examples:
# VITE_RESUME_FILE=resume.yaml          # Load public/resume.yaml
# VITE_RESUME_FILE=my-resume.yaml       # Load public/my-resume.yaml
# VITE_RESUME_FILE=resume-en.yaml       # Load public/resume-en.yaml

# Deployment Examples:
# VITE_ROOT_PATH=/                      # Deploy at domain root
# VITE_ROOT_PATH=/my-portfolio          # Deploy in subdirectory
# VITE_ROOT_PATH=/your-repo-name        # GitHub Pages subdirectory
