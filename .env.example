VITE_WEBSITE_TITLE=Mai
# Example Environment Variables File
# Copy this file as .env and modify as needed
# GitHub API Token (used for the Portfolio page)
VITE_GITHUB_USERNAME=...
VITE_GITHUB_TOKEN=...

# PIN Code Protection (used for the Resume page)
# If not set or left empty, PIN code verification is not required
VITE_PIN_CODE=123456

# Resume File Path Configuration
# Specify the YAML resume file to load
# If not set or left empty, the default example.yaml will be used
# VITE_RESUME_FILE=resume.yaml

# Usage examples:
# Load public/resume.yaml: VITE_RESUME_FILE=resume.yaml
# Load public/my-resume.yaml: VITE_RESUME_FILE=my-resume.yaml
# Use the default file: Leave this variable unset or set it to an empty value to automatically load example.yaml

# Custom Root Path (used for different deployment environments)
# Default value is '/', suitable for deploying at the domain root
# If deploying in a subdirectory, set the corresponding path (e.g., '/my-app')
# VITE_ROOT_PATH=/

# Deployment examples:
# Deploy in a subdirectory on GitHub Pages: VITE_ROOT_PATH=/your-repo-name
# Deploy in a custom subdirectory: VITE_ROOT_PATH=/portfolio
# Deploy at the root: VITE_ROOT_PATH=/ (or leave this variable unset)
